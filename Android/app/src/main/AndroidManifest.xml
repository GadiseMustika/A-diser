<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.adiser">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".authentication.PyApplication"
        android:allowBackup="true"
        android:icon="@drawable/home2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/home2"
        android:supportsRtl="true"
        android:theme="@style/Theme.Adiser">
        <activity
            android:name=".authentication.AuthenticationActivity"
            android:exported="true" />
        <activity
            android:name=".mainActivity.MainActivity"
            android:exported="true" />
        <activity
            android:name=".adminMainActivity.AdminMainActivity"
            android:exported="true" />
        <activity
            android:name=".SplashscreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>